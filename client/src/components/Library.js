import React, { useState, useEffect } from 'react';

function Library() {
    //filter state
    //game list state

    //should receive array of game objects save in state, but only pass the image and guid

    //useEffect that pulls from pagified game list, dependancy array contains the sorting filter.
    //depending on what filter is set to, (default: 'most reviewed' route) pull from a differently sorted index route

    return (
        <div class="library-wrapper">
            {/* map through the pagified game list and pass info as props to a 'game' component. */}
        </div>
    )

}

export default Library;